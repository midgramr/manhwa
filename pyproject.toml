[project]
name = "manhwa"
version = "0.1.0"
description = "A browser extension for translating raw manhwa"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "easyocr>=1.7.2",
    "pillow>=12.1.1",
]
